@model RepairPageViewModel

<h2>Repairs List</h2>
<table class="table">
    <thead>
        <tr>
            <th>RepairType</th>
            <th>PhoneModel</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var repair in Model.Repairs)
        {
            <tr>
                <td>@repair.RepairType</td>
                <td>@repair.PhoneModel.PhoneBrand @repair.PhoneModel.Name</td>
                <td>@repair.Price</td>
                @if (User.IsInRole("Admin"))
                {
                    <td>
                        <a asp-action="Edit" asp-route-id="@repair.RepairId" class="btn btn-primary">Edit</a>
                        <form asp-action="DeleteConfirm" asp-route-id="@repair.RepairId" method="post"
                              onsubmit="return confirm('Are you sure you want to delete this movie?');">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </td>
                }

            </tr>
        }
    </tbody>
</table>
@if (User.IsInRole("Admin"))
{
    <a asp-action="Create" asp-controller="Repair" class="btn btn-success">Add New Repair</a>
}

@* @model RepairPageViewModel
<h2 class="text-center">Select repair type</h2>
<div class="container mt-4">
    <div class="row">
        <!-- Decode Card -->
        <div class="col-md-6 mb-4">
            <div class="card decode-card">
                <div class="card-body">
                    <a href="/Repair/Decode" class="card-link text-decoration-none text-dark">
                        <h5 class="card-title">Decode</h5>
                    </a>
                    <p class="card-text">Description for Decode service.</p>
                </div>
            </div>
        </div>

        <!-- Display Change Card -->
        <div class="col-md-6 mb-4">
            <div class="card display-change-card">
                <div class="card-body">
                    <a href="/Repair/Decode" class="card-link text-decoration-none text-dark">
                        <h5 class="card-title">Display Change</h5>
                    </a>
                    <p class="card-text">Description for Display Change service.</p>
                </div>
            </div>
        </div>

        <!-- Battery Change Card -->
        <div class="col-md-6 mb-4">
            <div class="card battery-change-card">
                <div class="card-body">
                    <a href="/Repair/Decode" class="card-link text-decoration-none text-dark">
                        <h5 class="card-title">Battery Change</h5>
                    </a>
                    <p class="card-text">Description for Battery Change service.</p>
                </div>
            </div>
        </div>

        <!-- Protector Change Card -->
        <div class="col-md-6 mb-4">
            <div class="card protector-change-card">
                <div class="card-body">
                    <a class="navbar-brand" href="/Repair/Decode" class="card-link text-decoration-none text-dark">
                        <h5 class="card-title">Protector Change</h5>
                    </a>
                    <p class="card-text">Description for Protector Change service.</p>
                </div>
            </div>
        </div>
    </div>
</div> *@
